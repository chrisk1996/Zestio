version: '3.8'

services:
  propertypix-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: propertypix-pro
    restart: unless-stopped
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=production
      - PORT=3002
